version: "2"
services:
  mongo:
    image: mongo
    ports:
      - 27017:27017
  web:
    build:
      context: .
    command: sh -c "npm run docker-init_db && npm run docker-dev"
    ports:
      - 3000:3000
    depends_on:
      - mongo
    volumes:
      - .:/home/nodejs/app
