version: "2"
services:
    web:
        build: .
        ports:
            - 3000:3000
        environment:
            - DEBUG=True
        depends_on:
            - mongo
    mongo:
        image: mongo
        ports:
            - 27017:27017