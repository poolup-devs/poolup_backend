version: "3.4"
services:
  mongo:
    image: mongo
    ports:
      - 27017:27017
  web:
    build:
      context: .
    ports:
      - 3000:3000
    depends_on:
      - mongo
    volumes:
      - .:/app
